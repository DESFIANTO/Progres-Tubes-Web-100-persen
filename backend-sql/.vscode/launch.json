{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "Launch Program",
      "env": { "NODE_ENV": "localhost" },
      "program": "${workspaceFolder}/server.js"
    }
  ]
}
